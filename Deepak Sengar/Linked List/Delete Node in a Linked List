class Solution {
    public void deleteNode(ListNode node) {
        // here we dont have access to first node of head
        // No return type here
        node.val=node.next.val;
        node.next=node.next.next;
        
    }
}
//time compleity= O(1)
//space complexity=O(1)
